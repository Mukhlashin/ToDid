package cmd.ushiramaru.todid.data.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class ToDoViewModel(application: Application) : AndroidViewModel(application) {
}